"Refactor app.py to become a Workout Matcher for 'Couch Potatoes':

Visual Selection: Replace render_dashboard and render_log_workout with a 3-step visual card flow (Context, Gear, Muscle Group).

Instant Matching: Once a muscle group is selected, skip all forms and trigger a 4-minute workout immediately.

The Player: Create a dedicated workout view where the Looping GIF is the hero (80% of the screen).

No Writing/Logging: Remove all text inputs, difficulty sliders, and feeling selections. The goal is execution, not documentation.

Simplified Content: Filter exercises_data.py to provide 4 simple steps based on the Context (e.g., if Context is 'Office', use chair-based movements)."



The "Tinder-Gym" Total Overhaul Prompt
Context: The current Streamlit app feels like an outdated administrative form. We need a radical transformation into a high-end, mobile-first "Workout Matcher" that uses a Tinder-like UX to break the friction for "Couch Potatoes".

1. Visual Revolution (UI/UX):

Design Aesthetic: Implement a "Dark Glassmorphism" theme. Background: #0D1117, Cards: rgba(22, 27, 34, 0.7) with 12px blur, Accents: Neon Green (#00E676) and Electric Blue.

Component Overhaul: STOP using standard st.selectbox, st.radio, or st.sidebar. Replace them with Custom HTML/CSS Clickable Cards. Use st.components.v1.html or st.markdown with injected CSS to create large, tactile tiles.

The Matrix Flow: The home screen must be a clean, vertical scroll of large visual cards:

Context: (e.g., 🍿 Microwave Hero, 💻 Zoom-Proof Core, 👶 Kid-Toss Cardio).

Gear: (✋ Just me, 🎒 Basic Gear, 🏋️ Full Gym).

Muscle Map: Use the SVG from muscles.py but make it interactive—clicking a body part triggers a neon glow and starts the workout.

2. The Workout Player (Tinder Mode):

Hero Focus: Once an exercise starts, the Looping GIF must occupy 80% of the screen.

HUD Overlays:

Timer: A sleek, high-contrast circular or horizontal progress bar at the top.

Coaching: Display two prominent floating labels: ✅ DO: [Tip] and ❌ DON'T: [Mistake].

Zero-Form: Remove the "Save Workout" and "How did you feel" sliders. Log the workout automatically in the background upon completion.

3. Hybrid Intelligence (Garmin & Coach):

Silent Garmin: Keep the Garmin integration but hide the raw data. Use it as a Selection Engine: if Body Battery is low, auto-highlight the "Easy/Couch Potato" cards with a subtle pulse.

The Ghost Coach: The Coach shouldn't be a chatbox. It should be a Smart Overlay that appears only at the end of a workout to celebrate streaks or provide a single, punchy "Weekly Insight" from coach.py.

4. Technical Execution:

Refactor app.py: Create a clean state machine to handle the transitions between [Auth] -> [Selection] -> [Player] -> [Summary].

CSS Injection: Use the existing styles.css but update it with modern flexbox/grid layouts and 24px rounded corners for all elements.

Responsiveness: Every button must be "thumb-sized" (min 44x44px) with no vertical scrolling required during the workout player.